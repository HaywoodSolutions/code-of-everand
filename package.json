{
  "name": "code-of-everand",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "publish-all": "lerna publish"
  },
  "devDependencies": {
    "lerna": "^2.11.0"
  }
}