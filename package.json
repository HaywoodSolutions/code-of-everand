{
  "name": "@code-of-everand/root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "publish:registry": "lerna run publish:registry",
    "deploy": "lerna run deploy"
  },
  "devDependencies": {
    "lerna": "^2.11.0"
  }
}