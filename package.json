{
  "name": "code-of-everand",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "publish-all": "lerna publish"
  },
  "devDependencies": {
    "lerna": "^2.11.0"
  },
  "version": "0.0.0"
}
